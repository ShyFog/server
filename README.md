# Quick local installation guide

```bash
# Download v0.0.4 server to shyfog-server folder
git clone --recurse-submodules -b v0.0.4 https://github.com/ShyFog/server.git shyfog-server

# Install modules
cd shyfog-server
npm install

# Start the server
npm start
```